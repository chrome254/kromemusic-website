# Producer Krome — State Lab Studios

Static production website ready for GitHub Pages or Vercel. Set `SITE_URL` in `assets/app.js` to the deployed domain before launch.

Deploy: upload this folder to a Git repository, then import it into Vercel as a static site. No build command is required.
