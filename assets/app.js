const SITE_URL='https://www.kromemusic.co.ke';
const $=(s,root=document)=>root.querySelector(s);const $$=(s,root=document)=>[...root.querySelectorAll(s)];
const esc=s=>String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const link=(label,url)=>url?`<a class="stream" href="${esc(url)}" target="_blank" rel="noopener">${label} ↗</a>`:`<span class="stream disabled">${label}</span>`;
function releaseCard(r,kind='release'){return `<article class="${kind}-card"><div class="cover cover-${r.art}" role="img" aria-label="Artwork for ${esc(r.title)} by ${esc(r.artist)}"><span>${esc(r.year)}</span><b>${esc(r.title)}</b></div><div class="card-copy"><p class="kicker">${esc(r.artist)}</p><h3>${esc(r.title)}</h3><p class="muted">${esc(r.project)} · ${esc(r.roles.join(' · '))}</p><div class="streams">${link('Spotify',r.spotify)}${link('Apple Music',r.apple)}${link('YouTube',r.youtube)}</div></div></article>`}
function render(){const d=window.KROME_DATA;if($('#featured'))$('#featured').innerHTML=d.releases.slice(0,3).map(r=>releaseCard(r)).join('');if($('#music-grid'))$('#music-grid').innerHTML=d.releases.map(r=>releaseCard(r,'music')).join('');if($('#credits-grid'))$('#credits-grid').innerHTML=d.releases.map(r=>releaseCard(r,'credit')).join('');if($('#beat-grid'))$('#beat-grid').innerHTML=d.beats.map(b=>`<article class="beat-card" data-genre="${b.genre}" data-mood="${b.mood}" data-bpm="${b.bpm}" data-key="${b.key}"><div class="cover cover-${b.art}" role="img" aria-label="Abstract artwork for ${b.title}"><b>${b.title}</b><button class="preview" type="button" aria-label="Preview ${b.title}">▶</button></div><h3>${b.title}</h3><p class="muted">${b.genre} · ${b.mood}<br>${b.bpm} BPM · ${b.key}</p><strong>KES ${b.price}</strong><a class="button small" href="contact.html?subject=Beat%20licensing%20${encodeURIComponent(b.title)}">License beat <b>↗</b></a></article>`).join('')}
function filterCredits(){const query=$('#credit-search')?.value.toLowerCase()||'';const role=$('#role-filter')?.value||'';$$('.credit-card').forEach(card=>{const text=card.textContent.toLowerCase();card.hidden=!(text.includes(query)&&(!role||text.includes(role.toLowerCase())))});}
function filterBeats(){const genre=$('#beat-genre')?.value||'';const mood=$('#beat-mood')?.value||'';$$('.beat-card').forEach(c=>c.hidden=(genre&&c.dataset.genre!==genre)||(mood&&c.dataset.mood!==mood));}
function init(){render();const menu=$('.menu');const toggle=$('.menu-toggle');toggle?.addEventListener('click',()=>{const open=menu.classList.toggle('open');toggle.setAttribute('aria-expanded',open)});$('#credit-search')?.addEventListener('input',filterCredits);$('#role-filter')?.addEventListener('change',filterCredits);$('#beat-genre')?.addEventListener('change',filterBeats);$('#beat-mood')?.addEventListener('change',filterBeats);$('#contact-form')?.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget);const text=`Name: ${f.get('name')}%0AEmail: ${f.get('email')}%0AService: ${f.get('service')}%0A%0A${f.get('message')}`;window.open(`https://wa.me/254727946175?text=${encodeURIComponent(text)}`,'_blank','noopener')});const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.16});$$('.reveal').forEach(el=>observer.observe(el));}
document.addEventListener('DOMContentLoaded',init);
